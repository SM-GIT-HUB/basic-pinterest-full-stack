<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=0">
    <title>Pinteresting | Gallery Layout</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-white font-sans">
    <div class="flex justify-between items-center p-4 bg-white sticky top-0 z-50 shadow-md">
        <img src="/images/uploads/<%= user.dp %>" class="h-8 w-8 rounded-[50%]" alt="Pinterest Icon">
        <ul class="flex space-x-4">
            <a href="/feed"
                class="py-3 px-4 text-lg hover:bg-gray-800 hover:text-white transition-all rounded cursor-pointer border-[2px] border-black">Feed</a>
            <a href="/profile"
                class="py-3 px-4 text-lg hover:bg-gray-800 hover:text-white transition-all rounded cursor-pointer border-[2px] border-black">Profile</a>
            <a href="/logout"
                class="py-3 px-4 text-lg hover:bg-gray-800 hover:text-white transition-all rounded cursor-pointer border-[2px] border-black">Logout</a>
        </ul>
    </div>

    <div class="p-4 grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
        <% posts.forEach((elm)=> {
            %>
            <div class="bg-gray-100 rounded overflow-hidden aspect-w-4 aspect-h-3 flex flex-col justify-center p-[5px] rounded-[4px]">
                <img src="/images/uploads/<%= elm.image %>" class="object-cover rounded-[7px]" alt="Gallery Image">
                <h1 class="text-[20px] text-center font-semibold"><%= elm.title %></h1>
                <h1 class="text-[20px] text-gray-800 text-center font-semibold">@<%= elm.user.username %></h1>
            </div>
        <% }) %>
    </div>
</body>

</html>